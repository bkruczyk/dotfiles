export HISTFILE={{ shell.histfile }}
export HISTSIZE=5000
export SAVEHIST=5000

if [ -f {{ shell.secrets }} ]; then
   source {{ shell.secrets }}
fi

if [ -d {{ shell.localpath }} ]; then
  export PATH={{ shell.localpath }}:$PATH
fi
