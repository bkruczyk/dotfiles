;;; package --- Summary
;;; Commentary:

;;; Code:

(setq prelude-theme nil)
(setq prelude-auto-save nil)
(setq prelude-whitespace nil)                   ;; disable whitespace mode
(setq prelude-clean-whitespace-on-save nil)
(setq prelude-flyspell nil)                     ;; disable flyspell
(setq prelude-shell "/bin/bash")
(setq prelude-guru nil)

(provide 'preload)
;;; preload.el ends here
